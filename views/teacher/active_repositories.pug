extends ./includes/layout

block content
  .row
    h1.text-center #{student.lastname} #{student.firstname} #{student.middlename}
    .mx-auto.col-md-7.col-lg-6.col-sm-12
      div(role="tablist").list-group
        each repository in repositories
          a(href="/teacher/repository/"+repository._id, role="tab").list-group-item.list-group-item-action.bg-dark
            h3.text-primary #{repository.name}
            p.text-warning #{repository.updateAt}