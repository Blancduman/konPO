extends ./includes/layout

block content
  .row
    if student
      h2.text-white #{student.user.lastname} #{student.user.firstname} #{student.user.middlename}
    .mx-auto.col-md-7.col-lg-6.col-sm-12
      div(role="tablist").list-group
        if repositories
          each repository in repositories
            a(href='/teacher/active/' + student._id +'/' +repository._id, role="tab").list-group-item.list-group-item-action.bg-dark
              h3.text-primary #{repository.name}
              p.text-warning #{repository.updateAt}
        else 
          h2.text-primary Пока нет репозиториев